
<%# <h2>All animals for rent</h2>
<% @animals.each do |animal| %>
<%#   <%= animal.name %>
<%# <% end %> 

<h2>Ours lovely animals for rent</h2>

<div class="container">
  <div class="cards">
    <% @animals.each do |animal| %>
      <div class="card">
        <%= link_to animal.name, animal_path(animal) %>
      </div>
    <% end %>
  </div>
</div>
